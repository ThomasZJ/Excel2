<Window x:Class="Excel2.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Excel2" MinWidth="545" Width="1075" MinHeight="340" Height="800"  WindowStartupLocation="CenterScreen">

    <Grid Margin="0,0,0,0" Loaded="Grid_Loaded" Background="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="530" />
            <ColumnDefinition MinWidth="530"/>
        </Grid.ColumnDefinitions>
        <materialDesign:Card Padding="10" Margin="10,10,10,10" Grid.Column="0">
            <Grid>
                <TextBox x:Name="exclepath_textbox" Height="25" Margin="10,10,60,0" TextWrapping="Wrap" VerticalAlignment="Top" Text="" TextChanged="TextBox_TextChanged" LostKeyboardFocus="Textbox_LostFocus" PreviewDragOver="Textbox_DragEnter" PreviewDrop="Textbox_DragDrop">
                    <TextBox.Resources>
                        <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="Excel files path" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HintText}" />
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HintText}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox x:Name="jsonpath_textbox" Height="25" Margin="10,45,60,0" TextWrapping="Wrap" VerticalAlignment="Top" Text="" TextChanged="TextBox_TextChanged" LostKeyboardFocus="Textbox_LostFocus" PreviewDragOver="Textbox_DragEnter" PreviewDrop="Textbox_DragDrop">
                    <TextBox.Resources>
                        <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="json path" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HintText}" />
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HintText}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBox x:Name="templatefilepath_textbox" Height="25" Margin="10,80,60,0" TextWrapping="Wrap" VerticalAlignment="Top" Text="" TextChanged="TextBox_TextChanged" LostKeyboardFocus="Textbox_LostFocus" PreviewDragOver="Textbox_DragEnter" PreviewDrop="Textbox_DragDrop">
                    <TextBox.Resources>
                        <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="Template Classes files path" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HintText}" />
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HintText}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button x:Name="excelpath_btn" Content="..." Margin="0,10,10,0" VerticalAlignment="Top" Height="25" Click="Button_Click" HorizontalAlignment="Right" Width="45" Style="{StaticResource MaterialDesignRaisedDarkButton}" materialDesign:ButtonAssist.CornerRadius="5" />
                <Button x:Name="jsonpath_btn" Content="..." Margin="0,45,10,0" VerticalAlignment="Top" Height="25" Click="Button_Click" HorizontalAlignment="Right" Width="45" Style="{StaticResource MaterialDesignRaisedDarkButton}" materialDesign:ButtonAssist.CornerRadius="5" />
                <Button x:Name="templatefilepath_btn" Content="..." Margin="0,80,10,0" VerticalAlignment="Top" Height="25" Click="Button_Click" HorizontalAlignment="Right" Width="45" Style="{StaticResource MaterialDesignRaisedDarkButton}" materialDesign:ButtonAssist.CornerRadius="5" />

                <ListView x:Name="excelfile_listview" Margin="10,120,10,170" MouseLeftButtonUp="ListView_MouseClick">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ID}"/>
                            <GridViewColumn Header="FileName" DisplayMemberBinding="{Binding FileName}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <Button x:Name="begin_btn" Content="Begin" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="29" VerticalContentAlignment="Bottom" Margin="70,0,70,10" Style="{StaticResource MaterialDesignRaisedDarkButton}" materialDesign:ButtonAssist.CornerRadius="5" />

                <Label Content="Head" Margin="0,0,70,130" VerticalAlignment="Bottom" BorderBrush="#00000000" Height="26" HorizontalAlignment="Right" Width="47"/>
                <ComboBox x:Name="filternum_combobox" Margin="0,0,10,130" Height="26" SelectionChanged="ComboBox_SelectionChanged" Cursor="Arrow" HorizontalAlignment="Right" VerticalAlignment="Bottom" IsReadOnly="True" Width="60">
                    <Label Content="1" Background="#00000000" BorderBrush="#00000000" Width="50" />
                    <Label Content="2" Background="#00000000" BorderBrush="#00000000" Width="50" />
                    <Label Content="3" Background="#00000000" BorderBrush="#00000000" Width="50" />
                </ComboBox>
                <CheckBox x:Name="mutilsheet_checkbox" Content="Multi Sheet" HorizontalAlignment="Left" Margin="10,0,0,130" VerticalAlignment="Bottom" Checked="Multi_Checked" Unchecked="Multi_Checked" Height="26" />

                <RadioButton x:Name="dotcs_radiobtn" Content="csharp"  Margin="10,0,0,100"  HorizontalAlignment="Left" VerticalAlignment="Bottom" Checked="Radiobtn_Checked" Unchecked="Radiobtn_Checked"/>

                <RadioButton x:Name="dotts_radiobtn" Content="typescript" Margin="10,0,0,75" HorizontalAlignment="Left"  VerticalAlignment="Bottom" Checked="Radiobtn_Checked" Unchecked="Radiobtn_Checked"/>

                <TextBox x:Name="signsheet_textbox" HorizontalAlignment="Left" Margin="110,0,0,130" Width="90" Height="26" VerticalAlignment="Bottom" TextChanged="TextBox_TextChanged" >
                    <TextBox.Resources>
                        <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock FontStyle="Italic" Text="Exclude tags" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HintText}" />
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HintText}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <ProgressBar x:Name="progressbar" Height="5" Margin="0" VerticalAlignment="Bottom" Value="90" >
                </ProgressBar>
            </Grid>

        </materialDesign:Card>
        <materialDesign:Card 
                             Padding="10"
                             Margin="10,10,10,10" Grid.Column="1">
            <TabControl Grid.Column="1">
                <TabItem Header="Json" Width="auto">
                    <Grid Background="#FFE5E5E5">
                        <avalonEdit:TextEditor x:Name="jsontabitem" FontFamily="Consolas" SyntaxHighlighting="Json" FontSize="12pt" HorizontalScrollBarVisibility="Auto" IsReadOnly="True" ShowLineNumbers="True" VerticalScrollBarVisibility="Auto" WordWrap="True"/>
                    </Grid>
                </TabItem>
                <TabItem Header="Template" Width="auto">
                    <Grid Background="#FFE5E5E5">
                        <avalonEdit:TextEditor
                        x:Name="dotcsfiletabitem" 
                        FontFamily="Consolas"
                        SyntaxHighlighting="C#"
                        FontSize="12pt" HorizontalScrollBarVisibility="Auto" IsReadOnly="True" ShowLineNumbers="True" WordWrap="False" VerticalScrollBarVisibility="Auto">
                        </avalonEdit:TextEditor>
                    </Grid>
                </TabItem>
                <!--<TabItem Header=".h" Width="100">
                    <Grid Background="#FFE5E5E5">
                        <avalonEdit:TextEditor
                        x:Name="dothfiletabitem" 
                        FontFamily="Consolas"
                        SyntaxHighlighting="C++"
                        FontSize="12pt" HorizontalScrollBarVisibility="Auto" IsReadOnly="True" ShowLineNumbers="True" WordWrap="False" VerticalScrollBarVisibility="Auto"/>
                    </Grid>
                </TabItem>-->
            </TabControl>
        </materialDesign:Card>
    </Grid>
</Window>